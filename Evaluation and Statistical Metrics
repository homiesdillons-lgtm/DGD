def performanceStatistics(x, y):
    E = y - x  
    n = len(E)  
    ME = np.mean(E)  
    AE = np.abs(E)  
    MAE = np.mean(AE)  
    SE = np.power(E, 2)  
    RMSE = np.sqrt(MSE)  
    RB = ((np.mean(y) - np.mean(x)) / np.mean(x)) * 100 
    slope, intercept, r_value, p_value, std_err = stats.linregress(x, y)  


    R2 = r2_score(x, y)  
    NSE = 1 - (np.sum(np.power((x - y), 2)) / np.sum(np.power((x - np.mean(x)), 2)))  
    p_value_formatted = np.format_float_scientific(p_value, precision=10)
    print(f"原始p值: {p_value_formatted}")

    stat = {'R2': R2,
            'ME': ME,
            'RB': RB,
            'NSE': NSE,
            'MAE': MAE,
            'RMSE': RMSE,
            'pvalue': p_value_formatted,
            'n': n}
    return stat
